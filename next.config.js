/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "tailwindui.com",
      "images.unsplash.com",
      "illustrations.popsy.co",
    ],
  },
};

module.exports = nextConfig;
